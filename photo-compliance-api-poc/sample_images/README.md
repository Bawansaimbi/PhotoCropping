This folder is intended for **generated** sample images.

Run `python scripts/generate_sample_images.py` from the project root to create:

- `valid.jpg`
- `blurry.jpg`
- `low_resolution.jpg`
- `wrong_aspect.jpg`
- `busy_background.jpg`

These image files are ignored by git (see `.gitignore`) to avoid committing binaries.

